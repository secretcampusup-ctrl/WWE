# ThePornDB Integration

## الميزات المضافة

تم إضافة ميزة البحث في ThePornDB للتطبيق، تتيح لك:

1. **البحث عن الممثلات**: ابحث عن أي ممثلة واحصل على صورها عالية الجودة
2. **البحث عن المشاهد**: ابحث عن مشاهد محددة من الفيديوهات
3. **الضغط المطول على الفيديو**: عند الضغط المطول على أي فيديو، يظهر خيار "بحث ThePornDB"

## الملفات المضافة

1. **ThePornDBSettings.swift** - إعدادات الـ API ومفتاح المصادقة
2. **ThePornDBModels.swift** - نماذج البيانات (الممثلات، المشاهد)
3. **ThePornDBAPIService.swift** - خدمة الاتصال بـ ThePornDB API
4. **ThePornDBSearchView.swift** - واجهة البحث (الممثلات والمشاهد)
5. **ThePornDBSettingsView.swift** - واجهة إعدادات البحث

## مفتاح API

تم إضافة مفتاح API مدمج في الكود:
```
6r14sZOEGrWSD7L9E1MZkP4g1wNSGrSMyIWh5dOh5f271c34
```

### تغيير مفتاح API

إذا أردت استخدام مفتاح API خاص بك:

1. افتح `ThePornDBSettings.swift`
2. عدل قيمة المفتاح الافتراضي في دالة `apiKey`
3. أو استخدم واجهة الإعدادات للتطبيق لإدخال مفتاح جديد

## كيفية الاستخدام

### البحث عن ممثلة

1. افتح التطبيق
2. اضغط مطولاً على أي فيديو
3. اختر "بحث ThePornDB"
4. اختر تبويب "الممثلات"
5. اكتب اسم الممثلة
6. اختر الصورة المطلوبة

### البحث عن مشهد

1. افتح التطبيق
2. اضغط مطولاً على أي فيديو
3. اختر "بحث ThePornDB"
4. اختر تبويب "المشاهد"
5. اكتب اسم المشهد أو الممثلة
6. اختر الصورة المطلوبة

## الإعدادات

يمكنك تخصيص البحث من خلال الإعدادات:

- **عدد النتائج**: حدد عدد النتائج لكل بحث (افتراضي: 20)
- **جودة الصور**: اختر جودة الصور (منخفضة، متوسطة، عالية)
- **فلترة حسب السنة**: فلترة المشاهد حسب السنة

## API Endpoints

- **Base URL**: `https://api.theporndb.net`
- **الممثلات**: `/performers?q={query}&limit={limit}`
- **المشاهد**: `/scenes?q={query}&limit={limit}&year={year}`

## ملاحظات

- الميزة تعمل مع Cache لتحسين الأداء
- مدة الـ Cache: 15 دقيقة
- يدعم البحث باللغة الإنجليزية فقط
- يتطلب اتصال بالإنترنت
